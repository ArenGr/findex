{{-- Marks a nav entry whose page exists but has no content yet, so the menu
doesn't promise something the destination can't deliver. Same wording as the
/banks hub's card badges (see banks/index.blade.php). --}}
<span class="shrink-0 rounded-full bg-placeholder/60 px-1.5 py-0.5 text-[9px] font-semibold tracking-wide text-subtle uppercase">
    {{ __('offers.soon_badge') }}
</span>
